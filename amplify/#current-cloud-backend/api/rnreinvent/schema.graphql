type City @model {
    id: ID!
    name: String!
    country: String!
    locations: [Location] @connection
}

type Location @model {
    id: ID!
    name: String!
    description: String!
    address: String
}

